# Class named "character" for RPG characters

# properties
character_properties=()

# properties IDs
name=0
max_health=1
current_health=2
max_stamina=3
current_stamina=4
STR=5
PER=6
DEX=7
AGI=8
is_defending=9
is_on_fire=10
fire_time=11
is_smoked=12
smoke_time=13
caps=14


character.property() {
    if [ "$2" == "=" ]
    then
        character_properties[$1]=$3
    else
        echo ${character_properties[$1]}
    fi
}

character.name() {
    if [ "$1" == "=" ]
    then
        character.property name = "$2"
    else
        character.property name
    fi
}

character.max_health() {
    if [ "$1" == "=" ]
    then
        character.property max_health = "$2"
    else
        character.property max_health
    fi
}

character.current_health() {
    if [ "$1" == "=" ]
    then
        character.property current_health = "$2"
    else
        character.property current_health
    fi
}

character.max_stamina() {
    if [ "$1" == "=" ]
    then
        character.property max_stamina = "$2"
    else
        character.property max_stamina
    fi
}

character.current_stamina() {
    if [ "$1" == "=" ]
    then
        character.property current_stamina = "$2"
    else
        character.property current_stamina
    fi
}

character.STR() {
    if [ "$1" == "=" ]
    then
        character.property STR = "$2"
    else
        character.property STR
    fi
}

character.PER() {
    if [ "$1" == "=" ]
    then
        character.property PER = "$2"
    else
        character.property PER
    fi
}

character.DEX() {
    if [ "$1" == "=" ]
    then
        character.property DEX = "$2"
    else
        character.property DEX
    fi
}

character.AGI() {
    if [ "$1" == "=" ]
    then
        character.property AGI = "$2"
    else
        character.property AGI
    fi
}

character.is_defending() {
    if [ "$1" == "=" ]
    then
        character.property is_defending = "$2"
    else
        character.property is_defending
    fi
}

character.is_on_fire() {
    if [ "$1" == "=" ]
    then
        character.property is_on_fire = "$2"
    else
        character.property is_on_fire
    fi
}

character.fire_time() {
    if [ "$1" == "=" ]
    then
        character.property fire_time = "$2"
    else
        character.property fire_time
    fi
}

character.is_smoked() {
    if [ "$1" == "=" ]
    then
        character.property is_smoked = "$2"
    else
        character.property is_smoked
    fi
}

character.smoke_time() {
    if [ "$1" == "=" ]
    then
        character.property smoke_time = "$2"
    else
        character.property smoke_time
    fi
}

character.caps() {
    if [ "$1" == "=" ]
    then
        character.property caps = "$2"
    else
        character.property caps
    fi
}

# name max_hp max_stm
character.init() {
    if [ "$#" == "3" ]
    then
        character.name = "$1"
        character.max_health = "$2"
        character.current_health = "$(character.max_health)"
        character.max_stamina = "$3"
        character.current_stamina = "$(character.max_stamina)"

    else
        echo "Not enough/too many arguments were given in character.init function."
        exit 0
    fi
}

character.echo() {
    echo "Character $(character.name) has $(character.current_health)/$(character.max_health) HP, $(character.current_stamina)/$(character.max_stamina) STM"
}